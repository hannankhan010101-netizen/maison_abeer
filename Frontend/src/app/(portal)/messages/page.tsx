import { MessagesStudio } from '@/components/domain/MessagesStudio';

export const metadata = { title: 'Messages · Maison Abeer' };

export default function MessagesPage() {
  return <MessagesStudio />;
}
